# Open Questions

1. 是否要补跑完整 `/understand repos/langchain/libs/core --language zh`，用 LLM graph reviewer 生成更完整的代码图谱？
2. 是否要把 8 个 mini lab 从 spec 升级为可运行项目，并为每个 lab 增加 starter code、tests 和 reference implementation？
3. llm-wiki-skill 是否要直接改造为“代码研究包 ingest”，让 research pack 自动生成 entity/topic/source 页面？
4. `07-agent-loop` 是否要单独引入 LangGraph 作为第二阶段学习包，以避免把 LangChain v1 factory 和完整 workflow engine 混在同一课里？
5. 是否要把测试证据系统化补充到 `claims.json`，形成 code/test/docs/inferred 的自动证据矩阵？

