# AgentAction

<!-- confidence: EXTRACTED -->

`AgentAction` 是 LangChain Core 中表示 agent 工具调用意图的数据结构，源码位于 `libs/core/langchain_core/agents.py:44`。

## 关联

- 属于主题：[[agent-loop]]
- 选择 [[base-tool]] 并提供输入

## 证据

- `agents.py:44`：定义 `AgentAction`。
- `agents.py:51`：定义 `tool`。
- `agents.py:54`：定义 `tool_input`。
- `agents.py:58`：定义 `log`。

