# create_agent

<!-- confidence: EXTRACTED -->

`create_agent` 是 LangChain v1 的 agent 工厂函数，源码位于 `libs/langchain_v1/langchain/agents/factory.py:696`。

## 关联

- 属于主题：[[agent-loop]]
- 生成 compiled graph
- 使用模型、工具和中间件组成 agent workflow

## 证据

- `factory.py:696`：定义 `create_agent`。
- `factory.py:1670`：返回 compiled graph 相关逻辑。

