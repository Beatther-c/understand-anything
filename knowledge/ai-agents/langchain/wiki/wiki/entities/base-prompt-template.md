# BasePromptTemplate

<!-- confidence: EXTRACTED -->

`BasePromptTemplate` 是 LangChain Core 的 prompt 模板基类，源码位于 `libs/core/langchain_core/prompts/base.py:40`。

## 关联

- 属于主题：[[prompt-template]]
- 作为 [[runnable]] pipeline 的一步执行

## 证据

- `base.py:45`：定义 `input_variables`。
- `base.py:66`：定义 `partial_variables`。
- `base.py:160`：校验输入。
- `base.py:207`：定义 `invoke`。

